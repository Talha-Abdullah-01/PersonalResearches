SELECT ENAME AS NAME,SAL AS SALARY,SAL+300 AS INCREASE FROM EMP
SELECT ENAME "FIRST NAME",SAL "MONTHLY SALARY",SAL+300 AS INCREASE FROM EMP
SELECT * FROM EMP;
SELECT EMPNO,ENAME,SAL FROM EMP WHERE SAL<1000;
SELECT EMPNO,ENAME,SAL 
FROM EMP
WHERE HIREDATE<'01/JAN/1982';
SELECT * FROM ENAME,SAL FROM EMP WHERE SAL BETWEEN 1000 AND 15000;
SELECT * FROM EMP WHERE ENAME LIKE='SMITH';
SELECT * FROM EMPNO,ENAME,MGR FROM EMP WHERE MGR IN(7922,7082);
SELECT * FROM EMP WHERE SAL>=1000 AND SAL<=2000;
SELECT ENAME,SAL 
FROM EMP
WHERE SAL <=2000 AND (JOB='CLERK' OR JOB='ANALYST');

practice
DESC EMP;
SELECT EMPNO,ENAME,JOB,HIREDATE FROM EMP;

SELECT ENAME,SAL FROM EMP WHERE SAL>2850;

SELECT DISTINCT JOB FROM EMP;

SELECT ENAME,SAL FROM EMP WHERE SAL<1500 OR SAL>2850;

SELECT ENAME,JOB FROM EMP WHERE MGR IS NULL;

SELECT ENAME,SAL,COMM FROM EMP WHERE COMM IS NOT NULL ORDER BY SAL,COMM DESC;

SELECT ENAME FROM EMP WHERE ENAME LIKE '__A%';

SELECT HIREDATE,HIREDATE+1,HIREDATE-1 FROM EMP;

SELECT ENAME, ADD_MONTHS(HIREDATE,6) FROM EMP;

REVIEW FROM EMMP;
select ename "Name",ADD_MONTHS(HIREDATE,6) FROM EMP;

select ename, round(months_between(SYSDATE,HIREDATE)) AS MONTHS_WORKED
FROM EMP
ORDER BY MONTHS_WORKED ASC

SELECT MAX(SAL),MIN(SAL),AVG(SAL),SUM(SAL) FROM EMP;

SELECT COUNT(*) FROM EMP WHERE DEPTNO=30;

SELECT JOB, MIN(SAL), MAX(SAL) FROM EMP HAVING JOB NOT LIKE 'PRESIDENT' GROUP BY JOB ;

SELECT MAX(SAL) "MAXIMUM",MIN(SAL) "MINIMUM",SUM(SAL) "SUM", ROUND(AVG(SAL)) "AVERAGE"
FROM EMP;

SELECT JOB, MAX(SAL) "MAXIMUM",MIN(SAL) "MINIMUM",SUM(SAL) "SUM", ROUND(AVG(SAL)) "AVERAGE"
FROM EMP
GROUP BY JOB;

SELECT JOB,COUNT(JOB) FROM EMP
GROUP BY JOB;